const introReducer = function(state={player1: [], player2: []}, action){
  switch(action.type){
    case 'ADD_PLAYERS':
      debugger;
      return state
    default:
      return state;
  }
}

export default introReducer;
